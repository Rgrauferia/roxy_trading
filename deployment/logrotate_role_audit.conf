"""
Logrotate example for role_audit.log

Place this file on your server (e.g. /etc/logrotate.d/roxy_role_audit)
and adjust paths as needed.
"""
/path/to/roxy_trading/logs/role_audit.log {
    daily
    rotate 14
    compress
    missingok
    notifempty
    copytruncate
}
