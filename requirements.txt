pandas
# yfinance 1.5.1 still constructs generic NumPy timedeltas. NumPy 2.5
# deprecates that path and plans to turn it into an error; keep the latest
# compatible 2.4 line until yfinance removes the generic-unit conversion.
numpy>=2.0,<2.5
ccxt
yfinance
alpaca-py
ta
pytest
flake8
mypy

# formatting / dev
black
isort
# 1.59 fixes empty optional sidebar-theme colors; the browser probe validates
# Roxy's URL/sidebar navigation contract under the supported Python 3.12 runtime.
streamlit>=1.59.2,<1.60
streamlit-autorefresh
# Alpaca 0.43 still imports websockets.legacy. Version 13 satisfies Streamlit
# and Alpaca without the deprecation introduced by websockets 14.
websockets>=13,<14
matplotlib
plotly
playwright
APScheduler
feedparser
vaderSentiment
aiohttp>=3.13.5
urllib3>=2.6.3
requests>=2.31.0
# Pin filelock above the SoftFileLock TOCTOU fix. Python 3.12 is the supported
# local runtime; the production containers use Python 3.11.
filelock>=3.20.3
# CCXT 4.5.67 currently requires this exact build backend. The associated
# source-distribution-only advisory is classified separately from runtime risk.
setuptools==82.0.1

# Server API for voice assistant
fastapi
# Starlette 1.x uses the maintained httpx2 adapter for its ASGI TestClient.
httpx2>=2.7,<3
uvicorn[standard]
Flask>=3.1.3
openai>=0.27.0
faster-whisper
pypdf>=6.13.3
redis>=4.0.0

# LLM providers
elevenlabs>=2.0.0
anthropic
keyring
cryptography>=40.0
webauthn>=2.7.1
APScheduler

# Security floors compatible with the current Python 3.9 runtime.
tornado>=6.5.7
GitPython>=3.1.50
Werkzeug>=3.1.8
soupsieve>=2.8.4
idna>=3.15
